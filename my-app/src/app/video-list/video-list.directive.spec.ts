import { VideoListDirective } from './video-list.directive';

describe('VideoListDirective', () => {
  it('should create an instance', () => {
    const directive = new VideoListDirective();
    expect(directive).toBeTruthy();
  });
});
